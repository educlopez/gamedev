
import localFont from 'next/font/local'

// define a custom local font where GreatVibes-Regular.ttf is stored in the styles folder
const gameFont = localFont({ src: './../../public/fonts/04b03.woff2' })

export { gameFont }